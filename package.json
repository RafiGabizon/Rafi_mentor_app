{
  "name": "code-mentor-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.8.2"
  }
}
